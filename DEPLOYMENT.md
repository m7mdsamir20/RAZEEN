# دليل النشر — رزيم العقارية

خطوات رفع المشروع من GitHub إلى استضافة تدعم Node.js.

---

## 1. متطلبات الخادم

- **Node.js 20** أو أحدث
- **قاعدة بيانات MariaDB** (أو MySQL)
- إمكانية تشغيل أمر `npm` وتعيين متغيرات البيئة

---

## 2. قاعدة البيانات

أنشئ قاعدة بيانات MariaDB من لوحة الاستضافة، واحفظ:
اسم المستخدم، كلمة المرور، اسم القاعدة، والمضيف (host).

صيغة الرابط:

```
mysql://USER:PASSWORD@HOST:3306/DATABASE
```

> إذا كانت كلمة المرور تحتوي على رموز مثل `@` أو `#` أو `/`،
> فيجب ترميزها (URL-encode). مثال: `@` تصبح `%40`.

---

## 3. متغيرات البيئة

انسخ `.env.example` إلى `.env` على الخادم (أو أدخل القيم في لوحة الاستضافة).

**القيم التي يجب تغييرها عن التطوير:**

| المتغير | القيمة |
|:---|:---|
| `DATABASE_URL` | رابط MariaDB من الخطوة السابقة |
| `SESSION_SECRET` | **ولّد قيمة جديدة** — لا تستخدم قيمة التطوير |
| `NEXT_PUBLIC_SITE_URL` | دومين الموقع، مثل `https://razeemre.com` |
| `SMS_PROVIDER` | `authentica` بدل `console` |
| `AUTHENTICA_API_KEY` | مفتاح الرسائل الحقيقي |

> ⚠️ **المتغيرات التي تبدأ بـ `NEXT_PUBLIC_` تُدمج داخل الصفحات وقت البناء.**
> يجب إدخالها **قبل** أول عملية بناء — وإن غيّرتها لاحقاً فلن تسري إلا بعد
> إعادة بناء المشروع.

لتوليد مفتاح جلسات جديد:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## 4. تقييد مفاتيح خرائط جوجل

مهم قبل أن يصبح الموقع عاماً — وإلا يستطيع أي شخص استخدام مفاتيحك
وتصلك الفاتورة.

من [Google Cloud Console](https://console.cloud.google.com/apis/credentials):

- **`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`** — قيّده بـ *HTTP referrers* على
  دومين موقعك، وفعّل عليه **Maps JavaScript API** فقط
- **`GOOGLE_MAPS_SERVER_KEY`** — قيّده بـ *IP addresses* على عنوان
  الخادم، وفعّل عليه **Geocoding API** و **Places API**

---

## 5. البناء والتشغيل

```bash
npm ci
npm run db:push
npm run build
npm start
```

- `npm ci` يثبّت الحزم، ويشغّل `prisma generate` تلقائياً بعدها
- `npm run db:push` ينشئ الجداول في قاعدة البيانات
- `npm start` يشغّل الخادم على المنفذ 3000 افتراضياً

لتغيير المنفذ: `PORT=8080 npm start`

> **لا تشغّل `npm run db:seed` على الإنتاج** — فهو يضيف حسابات وعقارات
> تجريبية بكلمة مرور معروفة.

---

## 6. حساب الإدارة

1. افتح الموقع وأنشئ حساباً جديداً بالاسم **«رزيم العقارية»**
2. رقّ الحساب إلى إدارة:

```bash
npm run make:admin 05XXXXXXXX
```

3. **سجّل خروج ثم دخول** — الصلاحية تُقرأ من الجلسة، ولا تتغير قبل
   تسجيل دخول جديد

للتراجع: `npm run make:admin 05XXXXXXXX -- --revoke`

---

## 7. مجلد الرفع

يجب أن يكون `public/uploads/` موجوداً وقابلاً للكتابة، فالصور
والفيديوهات تُحفظ فيه.

```bash
mkdir -p public/uploads/properties
chmod 755 public/uploads
```

> إن كانت الاستضافة تعيد بناء المشروع من الصفر مع كل نشر، فاحرص
> على أن يكون هذا المجلد على تخزين دائم لا يُمسح مع البناء.

---

## 8. التحديثات اللاحقة

```bash
git pull
npm ci
npm run db:push    # فقط إذا تغيّر ملف schema.prisma
npm run build
```

ثم أعد تشغيل التطبيق من لوحة الاستضافة.

---

## ملاحظات

- **التطوير المحلي يحتاج MariaDB أيضاً** بعد هذا التحويل. ثبّت MariaDB
  محلياً، أو وجّه `DATABASE_URL` إلى قاعدة بيانات تطوير منفصلة على
  الاستضافة إن كانت تسمح بالاتصال الخارجي.
- **`.env` لا يُرفع إلى Git** إطلاقاً — القيم تُدخل على الخادم مباشرة.
- **رمز التحقق يظهر في الشاشة** فقط عندما يكون `SMS_PROVIDER=console`
  وخارج بيئة الإنتاج. في الإنتاج يصل كرسالة نصية فعلية.
